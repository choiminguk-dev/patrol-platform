# 2026-04-16~17 — 얼굴 블러 재활성화 + AI 그룹핑 정확도 개선

## 핵심 성과

### 1. 얼굴 블러 재활성화 (d0fb0f6 ~ 9ad35db)
- face-api import 복구 (FileList race가 진짜 원인이었으므로 안전하게 복귀)
- **2단 블러**: 6블록 모자이크 → Gaussian 블러 2패스 → 식별 완전 불가
- CDN 10s + 모델 15s 타임아웃 → 실패 시 블러 없이 업로드 계속
- 썸네일에도 블러 반영 (preparePhotos 완료 후 blurredBlob으로 재생성)
- 서버 sharp 파이프라인도 동일 강도 (margin 0.3, 6px→원크기→blur)
- 클라이언트 블러 강도 상향: blurPx max(30, min/3) × 2패스 → 서버 수준

### 2. AI 그룹핑 정확도 대폭 개선 (802597e ~ 4cb82a5)

#### 모델 업그레이드
- `claude-haiku-4-5` → `claude-sonnet-4-6` (비전 OCR 정확도 대폭 향상)
- max_tokens 2000 → 4000

#### 프롬프트 개선 (d681471)
- "의심스러우면 합치지 말고 나누세요" 최상단 배치
- 그룹 기준 3~6장 → 2~5장
- 시각적 분할 단서 추가 (건물/간판/도로 변화 = 다른 위치)
- 랜드마크 매칭 ("남산마트 간판 → 해당 구역")
- "같은 구역 여러 번 방문 가능" 명시

#### 후처리 버그 수정
| 이슈 | 원인 | 수정 |
|---|---|---|
| 27장→1건 합침 | 후처리4: 같은 zoneId면 전부 머지 | zone + 주소 동일만 → 완전 제거 |
| 다른 방문 합침 | 크로스-청크 병합 (같은 주소 다른 시간대) | 후처리4 완전 제거 (6353032) |
| 사진 1+10 엉뚱 합침 | 1장 그룹 무조건 인접 흡수 | 주소 있는 1장만, 앞 작업사진에만 |
| 연속 주소판 오배정 | [작업]→[주소A]→[주소B] 시 A가 합쳐짐 | 앞→뒤 순회, 뒤 주소판 우선 (e827620) |
| 같은 zone 분리됨 | 인접 같은 zone 병합 없음 | 후처리 2.3 추가 (bb3b00f) |
| 같은 주소 원거리 합침 | 무조건 합침 | 사진 번호 3장 이내만 |

#### CHUNK_SIZE 탐색
- 15 → 10 → 5 → **10 (최종)**: 10이 AI 그룹핑 정확도 최적점
- 5는 과분할 (너무 잘게 쪼갬), 15는 과소분할

#### 기타 개선
- API 에러 방어: 청크별 try-catch (한 청크 실패 시 나머지 계속)
- 서버 로그: `[analyze] chunk N-M: AI returned X groups` + 후처리 결과

### 3. 현재 그룹핑 정확도
- 27장 테스트: ~80% 정확 (11그룹 중 2~3개 수동 보정 필요)
- 주요 남은 이슈: 같은 청크 내 시각적 분할 한계 (AI 인식 문제)
- 리뷰 화면의 분할/병합 기능으로 2~3분 내 보정 가능

## 프로덕션 상태

- 최종 배포 커밋: `4cb82a5`
- patrol.ai.kr 정상 운영
- 로컬 Docker DB에 zone 없음 (프로덕션 RDS에만 존재)

## 시연 준비 (04-17)

- 대상: 스마트정보과장(5급) + 통신직 주무관
- 시나리오: `C:\Users\user\Downloads\스마트정보과장_시연시나리오.docx`
- 핵심: "업무가 바뀌는 장면" 보여주기 (기술 설명 X)
- 팁: Win+Ctrl+D로 새 가상 데스크톱 → patrol.ai.kr만 띄워서 시연

## 서버 운영 명령어

```bash
# SSH 접속
ssh -i C:\Users\user\Downloads\patrol-key.pem ubuntu@133.186.218.27

# 코드 업데이트
cd ~/patrol-platform && git pull origin main && npm run build && sudo systemctl restart patrol

# 로그 확인 (실시간 필터)
sudo journalctl -u patrol -f --no-pager | grep --line-buffered -E 'analyze|error|Error'

# 최근 로그
sudo journalctl -u patrol -n 200 --no-pager | grep -E 'analyze|error|Error'
```

## 커밋 이력 (이번 세션)

```
4cb82a5 fix: CHUNK_SIZE 10 복원 — 최적 AI 그룹핑
e827620 fix: 연속 주소판 병합 순서 — 마지막 주소판 우선
f0ea76b perf: CHUNK_SIZE 10→5
6353032 fix: 크로스-청크 병합(후처리 4) 제거
bb3b00f fix: 3번째 청크 에러 방어 + 인접 같은 zone 병합
fafb4ee fix: 그룹핑 정확도 추가 개선 — 해상도 복원 + 후처리 보수화
d681471 fix: 그룹핑 프롬프트 강화 — 과소 분할 방지 + 랜드마크 매칭
802597e perf+fix: AI 그룹핑 정확도 + 속도 개선 (Sonnet + 해상도 조정)
9ad35db feat: 클라이언트 블러 강도 상향
63779ad feat: 썸네일 그리드에도 얼굴 블러 반영
d0fb0f6 feat: 얼굴 감지 + 블러 재활성화
9f2d80b docs: 업로드 hang 근본원인 기록
65ece91 fix: 업로드 FileList 클리어 레이스 수정
987e46d safety: compressImage/createThumbnail 타임아웃
05b0c07 cleanup: preloadFaceApi import 제거
```

## 다음 작업 후보

1. 그룹핑 정확도 추가 개선 (2-pass: 1차 주소판 식별 → 2차 그룹핑)
2. 주소판 감지 재도입 (address-sign-detect.ts 활용)
3. 프롬프트 캐싱 (비용 절감)
4. 시연 후 피드백 반영
